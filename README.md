# Spidy
Custom Environment for RL

This project implements the Q-learning method using an Custom Environment from OpenAi Gymnasium.

The agent (spiderman) is learning to get to the goal state (Gwen) avoiding the hell states (Villains).
